
@model IEnumerable<Hanashop.Models.CHUDE>

<h3 style="Text-align: center; color: white"> Chủ Đề </h3>

@foreach (var sach in Model)
{
    <div class="col-sm-4 col-lg-4 col-md-4">
        <div class="thumbnail">
            <a href="@Url.Action("SPTheochude", "Trangchu", new { id = sach.MaCD })">

                <div>
                    <br />
                    <h4 style="text-align: center;color:red">
                        @sach.TenChuDe
                    </h4>
                    <p></p>
                </div>

        </div>
    </div>
}


